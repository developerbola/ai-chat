name: Keep Supabase Awake

on:
  schedule:
    - cron: "0 */12 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Supabase
        run: |
          curl -X GET \
            "https://dyflmrrsepsnhwonbpdh.supabase.co/rest/v1/chats?select=chat_id&limit=1" \
            -H "apikey: sb_publishable_DQv8ivnGuM9G7jZci8qDIQ_tAJowx2B" \
            -H "Authorization: Bearer sb_publishable_DQv8ivnGuM9G7jZci8qDIQ_tAJowx2B" \
            -H "Content-Type: application/json"
